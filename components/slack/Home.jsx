import React, { useState } from "react";

const Home = () => {
  const [user, setUser] = useState("");

  return (
    <div>
      {/* if (user) aka if we are logged in, show courses */}

      {/* if not user, aka not logged in, show login/register */}
    </div>
  );
};

export default Home;
